[package]
name = "space_shooter"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.19.1"
bevy_embedded_assets = "0.16.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
